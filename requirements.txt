# Core dependencies
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.8.0

# PyTorch and PyTorch Geometric
# torch and torch-geometric are installed separately in the Dockerfile
# (CPU-only wheel) before this file is processed, so they are intentionally
# absent here to avoid re-downloading the GPU build.
# torch-scatter / torch-sparse are NOT needed: all conv layers used
# (SAGEConv, GATConv, GCNConv, GINConv, HeteroConv) work natively in PyG >=2.3.

# ML models
xgboost>=2.0.0

# MLOps
mlflow>=2.13.0

# API serving
fastapi>=0.109.0
uvicorn>=0.27.0

# Dashboard
streamlit>=1.31.0
plotly>=5.18.0

# Data fetching
requests>=2.31.0
python-dotenv>=1.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Optional: For GPU support
# torch-cuda  # If using CUDA
# torch-tensorrt  # For TensorRT optimization
# Core dependencies
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# PyTorch and PyTorch Geometric
# Note: torch-scatter / torch-sparse are intentionally NOT pinned here.
# PyG >=2.3 implements scatter/segment ops natively via torch.scatter_reduce,
# and the conv layers used in this project (SAGEConv, GATConv, GCNConv,
# GINConv, HeteroConv, global_mean_pool) need no compiled extensions.
# Pinning them breaks `pip install` because their setup.py imports torch
# before torch is available in the same build pass.
torch>=2.0.0
torch-geometric>=2.3.0

# ML models
xgboost>=2.0.0

# MLOps
mlflow>=2.13.0

# API serving
fastapi>=0.109.0
uvicorn>=0.27.0

# Dashboard
streamlit>=1.31.0
plotly>=5.18.0

# Data fetching
requests>=2.31.0
python-dotenv>=1.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Optional: For GPU support
# torch-cuda  # If using CUDA
# torch-tensorrt  # For TensorRT optimization